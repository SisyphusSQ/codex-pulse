# M7-E5 Quota 来源、仲裁与 Reset Credits 页面验证 Runbook

## 当前验证结果

- 记录时间：2026-07-17
- 本轮任务：TOO-276 `/quota` typed current、redacted manual refresh、来源/仲裁/credits/失败状态
- 当前结论：全部验证、两层 review、CHANGELOG、PR #44、自合并与 `main` post-merge verify 均已完成；TOO-276 已回写 Linear Done，merge commit 为 `1b33e10e980712a545f8c39d390d1be288ecaba6`
- 对应计划：`.agents/plans/2026-07-17-too-276-quota-page.md`
- GitHub Actions：`actions_disabled_by_user`，不查询、不触发、不等待 CI
- Release：普通 Execution，不创建 tag、GitHub Release、appcast 或正式发布产物

## 目标

验证 `/quota` 只消费 generated `QuotaCurrentResponse` 与 redacted `RequestQuotaRefresh` receipt，完整区分 trusted/stale/expired/suspicious/conflict/unavailable、unknown/真实零、Reset Credits 及 quota/reset-credits refresh 状态；不泄露 token、path、claim/request/observation identity 或 raw error。

## 执行副作用

- Go/frontend tests、typecheck/build 会写 Go/npm/Vitest/Vite cache、ignored `frontend/node_modules/`、`frontend/dist/`、`.task/` 与 `bin/`。
- generated gate 会重生成 `frontend/bindings/` 并比较 tracked truth；失败时必须恢复为一致状态。
- visual QA 只使用不进入 production route 的 typed synthetic DTO fixture，在 `.agents/runs/too-276-quota-qa/` 写 ignored screenshot/comparison。
- packaged QA 使用 isolated HOME/TMP；不读取真实 Codex Home/auth，不访问 Wham，不发送网络请求；结束后停止 app 并清理隔离目录。
- 不访问 GitHub Actions，不执行 release。

## 前置条件

1. branch=`suqing/too-276-quota-page`，baseline=`main@9b3afae`。
2. TOO-272 与 M5/M6 Quota current/runtime/query contract 已在 baseline。
3. source=`docs/design/front/previews/07-quota.png`；实现只适配真实 DTO。
4. online toggle 属 TOO-277；静态 Review/Fast tasks 不作为本卡数据源。
5. 所有 HTTP/credential 场景必须使用 synthetic transport 或 typed DTO，不触达真实用户服务。

## Acceptance Matrix

| 验收面 | 入口 | 通过条件 |
| --- | --- | --- |
| Wails command | `internal/app` tests + generated contracts | finite source、runtime delegate、redacted receipt、content-free error |
| current states | quota formatter/view tests | trusted/stale/expired/suspicious/conflict/unavailable |
| unknown/zero | window/credits tests | null 不画假值，真实 0 可辨 |
| sources/evidence | cards/view tests | fixed enums/time/value；identity/raw cause不进DOM |
| refresh | mutation tests | quota+credits、pending disable、settled invalidate、typed failure反馈、backend throttle权威 |
| a11y | component/view tests | labels、progress、live status、focus/retry |
| visual/native | source comparison + isolated package | 1440、900 minimum、1120 packaged，无伪数据/溢出 |

## 步骤

### 1. Binding 与 focused frontend

```bash
go test ./internal/app -run 'TestBinding|TestWailsBinding' -count=1
npm --prefix frontend test -- --run src/features/quota src/views/QuotaView.test.ts src/router/index.test.ts src/bindings/contracts.test.ts
npm --prefix frontend run typecheck
git diff --check
```

### 2. Generated 与 repository gate

```bash
wails3 generate bindings -clean=true -ts -i
make harness-verify
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify-project
/usr/bin/python3 .agents/skills/project-version-release/scripts/project_version_release.py check --repo "$PWD" --json
```

### 3. Post-integration full gate

```bash
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
go mod tidy -diff
CGO_ENABLED=0 go test ./internal/codex/quota ./internal/scheduler ./internal/store ./internal/store/sqlite -count=1
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify
git diff --check
```

### 4. Visual / native

1. 注入 isolated typed DTO fixture，生成 normal、stale/conflict、unavailable/failed 三类状态。
2. 1440×1024 与 source 合并对照，900×600 检查堆叠与 overflow，1120×720 packaged 检查原生窗口。
3. 删除 fixture，isolated HOME/TMP 验证真实 binding unavailable/fatal/retry，不触达 auth/Wham。
4. 停止进程并清理隔离目录，仅保留 ignored 最终证据。

## 失败与恢复

| 失败点 | 停止条件 | 恢复 |
| --- | --- | --- |
| binding leak/invalid source | 不进入 UI review | 保留 RED，收窄 receipt/validation |
| refresh network fixture | 不访问真实网络 | 只修 synthetic transport/DTO 路径 |
| unknown/0 混淆 | 当前 slice 停止 | 修 formatter/ARIA 并 focused repeat |
| generated drift | 不提交 | 重新生成并核对 tracked/untracked |
| final scope finding | 不 commit/PR | 修复、受影响 verify、同 reviewer复核 |

## 清理

- 删除 production route 外的 typed fixture。
- 停止 Wails/Vite/app process，清理 isolated HOME/TMP。
- 不删除已有用户 `.agents/state/plans/runs`；ignored QA evidence按计划保留。

## 结果记录

### Pre-review focused

- `go test ./internal/query ./internal/app -count=1`：PASS；当前 macOS SDK 对 Wails 上游 deployment 的既有 linker warning 不影响测试结论。
- quota/binding/router focused：首轮 6 files / 17 tests PASS；覆盖 unknown/真实零、reset_at/倒计时、Local/Wham observation/evidence、401/429/network/schema、空/加载/partial/stale/fatal、手动刷新 settled invalidation 与 scope dispose cancellation。
- frontend full：首轮 39 files / 106 tests PASS；`npm run build:dev`、typecheck PASS。
- `make harness-verify`、`make verify-project`、版本策略 check、`make verify-generated` 与 `git diff --check`：PASS；生成结果稳定为 1 service / 16 methods / 35 enums / 69 models / 1 event。

### Browser visual

- 使用 synthetic typed DTO 和本机 Vite transport fixture，不读取真实 Home/auth，不访问 Wham。
- `1440×1024` source comparison：PASS；保留 source 设计的标题、双窗口、来源/credits/诊断层级，同时移除不属于本卡的 online toggle 与静态 Review/Fast task。
- `900×600` minimum：PASS；`body clientWidth=900`、`scrollWidth=900`，无横向 overflow，2 个窗口和 2 个来源保持可达。
- 刷新动作在真实浏览器触发两类 generated command，console 0 error；唯一 warning 是 Wails 对 browser preview 的预期提示。
- ignored evidence：`.agents/runs/too-276-quota-qa/quota-1440x1024.png`、`quota-900x600.png`。

### Independent implementation review

- 首轮 reviewer 发现 2 项 blocking：不可信 future `resetsAtMs` 被推导成倒计时；重叠 refresh 只取消最后批次。
- 两项均先以回归测试复现 RED，再修复为：`resetRemainingMs` 是倒计时可信门槛；`Set` 追踪并取消所有在途 Wails commands。
- 修复后 focused 6 files / 18 tests、typecheck、`git diff --check` PASS；同一 reviewer closure review 的 P0/P1/P2 均为 0，`IMPLEMENTATION_REVIEW_PASS: YES`。
- reviewer 的非阻塞 P3：当前 fixture 直接覆盖 expired future reset，suspicious 由同一个 `resetRemainingMs` 守卫处理，但未另建同义 fixture；记录为残余测试增强，不改变本卡通过结论。

### Post-integration full / package

- `go test ./... -count=1`、`go test -race ./... -count=1`、`go vet ./...`、`go mod tidy -diff`：PASS；Wails 上游 macOS deployment linker warning 为既有环境提示。
- frontend full：39 files / 107 tests PASS；typecheck、production build PASS。
- Pure-Go 核心：quota/scheduler/store/store-sqlite 在 `CGO_ENABLED=0` 下 PASS，实际依赖链未命中 `gorm.io/driver/sqlite` 或 `github.com/mattn/go-sqlite3`。全仓或 `internal/app` 不使用 CGO-off，因为 Wails macOS adapter 按 build constraints 需要 CGO。
- `PATH=/tmp/codex-pulse-tools/bin:$PATH make verify`：PASS；harness/project/generated stable（1 service / 16 methods / 35 enums / 69 models / 1 event）、arm64 minOS 15.0.0 ad-hoc app 与 ZIP 全部验证通过。
- 修复前的同一生产 bundle 已在 isolated HOME/TMP 以 1120×720 原生窗口验证 shell 与 unavailable/fatal 路径；修复后重新 package/verify PASS。修复仅收紧倒计时可信门槛与取消集合，不改变窗口壳、路由或打包配置。

### CHANGELOG gate

- 已在 `CHANGELOG.md -> Unreleased -> feature` 写入 `[TOO-276]` 已完成事实；未修改历史 release、版本、appcast 或 GitHub Actions。
- changelog 后 `git diff --check`、`make harness-verify` 与版本策略 JSON check 均 PASS。

### Final scope review

- 不同 subagent 只读核对完整 tracked/untracked diff、Included/Excluded、CHANGELOG、secret、version/release/Actions 与原子性。
- P0/P1/P2 均为 0，`blocking_findings: []`，结论 `READY_FOR_INTEGRATION`。
- 非阻塞 P3 与 implementation review 一致：可另补 suspicious future-reset 同义 fixture；现有 expired fixture 已覆盖同一可信门槛，不阻断。

### 最终 closeout

- PR #44 已由 Codex 自行创建并 squash merge，`main` merge commit 为 `1b33e10e980712a545f8c39d390d1be288ecaba6`。
- post-merge 验证与协作结果回写均完成，TOO-276 已置为 Done；GitHub Actions 保持停用，未执行正式发布。
- 命令失败时仅记录已执行步骤、失败摘要和下一动作；未执行步骤不得标 PASS。
