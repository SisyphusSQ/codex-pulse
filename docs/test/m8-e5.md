# M8-E5 Data Health 页面与恢复动作 Runbook

## 当前验证结果

- 记录时间：2026-07-18
- 当前结论：`PASS`（等待独立评审与 PR closeout）
- 对应 Issue / 分支：TOO-283 / `suqing/too-283-data-health`
- Actions：`actions_disabled_by_user`，不查询、不触发、不等待
- Release：普通 Execution，不创建 tag、GitHub Release、appcast 或正式发布产物

## 目标

- 证明 Data Health 只消费最近 24 小时 typed Store/query 与 Health Projection，不从日志、单一指标或浏览器聚合推断健康。
- 证明 source/index/storage/quota/updater 等七组件、当前工作、合并事件和 CPU/RSS/DB/WAL/磁盘/queue 有真实 provider。
- 证明 recovery registry 只执行重取、reconcile、Session index Analyze-only dry-run 或 Settings 导航；未知值 fail closed。
- 证明每个 command 有安全预览、pending、有限 receipt、失败语义和完成后权威 refetch。

## 执行副作用

- Go/Vue 测试只使用 synthetic fixture、临时 SQLite 与缓存；不会打开默认应用数据库或真实 Codex Home。
- frontend build 写 ignored `frontend/dist/`；bindings 生成更新提交版 `frontend/bindings`。
- package gate 生成 ignored ad-hoc `bin/Codex Pulse.app` 与 ZIP，不签 Developer ID、不公证、不发布。
- 不触发网络、GitHub Actions、生产系统或正式发布；不读取或写入凭据。

## Acceptance Matrix

| 面 | 验证入口 | 通过条件 |
| --- | --- | --- |
| DataHealth query | Go unit/binding tests | 精确 24h；最多 289 趋势点且保留两端；queued/running/未消费 interrupted 与 open 优先、窗口外历史裁剪、activity 总量有界；空态/取消/非法 snapshot fail closed |
| Generated contract | app/bindings tests | 1 service / 24 methods / 41 enums / 88 models / 1 event；DataHealth 是 query |
| 页面 | Vue unit/integration | 七组件、Source/scheduler、Job/event occurrence、CPU/RSS/DB/WAL/磁盘/queue 完整 |
| 状态 | Vue query/view tests | DataHealth 与 Projection 分别标时；loading、known empty、partial/unavailable、last-trusted、取消与重复动作均有界 |
| Actions | registry/view tests | 只显示 projection 声明的有限动作；preview/progress/result/error/refetch 完整 |
| 隐私/范围 | static/review | 无 opaque identity/path/raw error/content/token；无 repair execute/Actions/release |

## Development Funnel

```bash
CGO_ENABLED=0 go test ./internal/query/runtimeinfo -count=20
go test ./internal/app -count=20
go test -race ./internal/query/runtimeinfo ./internal/app -count=10
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify-generated
git diff --check
```

## Integration Funnel

```bash
go test -p=1 ./... -count=1
go test -race -p=1 ./... -count=1
go vet ./...
go mod tidy -diff
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
make harness-verify
PATH="/tmp/codex-pulse-tools/bin:$PATH" make project-check
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify
git diff --check
```

## 场景步骤

1. 用 synthetic GORM/Store MetricsSnapshot 构造普通与 Detailed 24h 采样，核对时间窗、289 点上限、两端证据、JS-safe 数值和取消。
2. 用 typed DataHealth activity 与 Projection DTO 驱动页面，核对七组件 evidence、source/scheduler 指标、current/open 优先且窗口外历史不展示、真实 Job phase、event occurrence 和资源趋势。
3. 将 projection recovery action 依次设为 none/retry/check_source/grant_permission/free_space/choose_home/repair_store/unknown，核对只出现已注册映射。
4. 对 reconcile 与 repair dry-run 核对预览、pending、有限 receipt、失败不关闭事件，以及 settle 后 Source/Job/Health/Data Health refetch。
5. 在 900×600、1120×720 和 1440×1024 结构约束下复核二级 route、无独立主导航、单 Banner 与隐私 DOM。

## 当前结果记录

| 步骤 | 结果 | 备注 |
| --- | --- | --- |
| scope/branch/lease | PASS | normal risk；独立分支和 write lease 已建立 |
| RED→GREEN | PASS | resource contract、binding、query cache、route、registry、view、入口与趋势均先观察预期失败 |
| focused | PASS | runtimeinfo 重复 20 次与 race 20 次、app binding race 20 次通过；Data Health、入口、路由、query、trend 等相关前端场景通过；current/open 优先、24h 裁剪、真实 Job phase、独立时间、焦点闭环与 partial/last-trusted 已覆盖 |
| integration | PASS | Go 全量与全仓 race、vet/tidy、52 个前端文件 155 项测试、typecheck/build、harness/project-check/generated/package 全部通过；高压力 app 重复曾出现两次既有 Home Switch 时序 flake，对应用例单独 50/30 次及标准全量/race 门禁均通过 |
| implementation review | PASS | 独立 reviewer 首轮发现 4 项阻塞缺口；Projection 空态、current/open 分页、跨 query 时间、真实 Job phase 与 consumed-interrupted lineage 均已修复并由原 reviewer closure PASS |
| changelog/final scope | PASS | 已按 changelog-style 在 Unreleased 写入单条已完成事实；不同 subagent Final Scope Review 确认无缺失验收、范围漂移、敏感信息或原子提交集问题 |
| PR/merge/post-merge | 未执行 |  |

执行后只回写脱敏摘要；不得写入原始错误、绝对临时路径、真实用户标识或凭据。
