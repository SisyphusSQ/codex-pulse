# M8-E4 本机状态健康入口与单 Banner Runbook

## 当前验证结果

- 记录时间：2026-07-18
- 当前结论：`PASS；PR #51 已 self-merge，main post-merge 完整验证通过，TOO-282 已 Done`
- 对应 Issue / 分支：TOO-282 / `suqing/too-282-health-entry-banner`
- Actions：`actions_disabled_by_user`，不查询、不触发、不等待
- Release：普通 Execution，不创建 tag、GitHub Release、appcast 或正式发布产物

## 目标

- 证明主窗口只消费 TOO-280 的真实七组件 Health Projection，不从事件数量或日志推断健康。
- 证明本机状态入口显示总体状态点和有限组件摘要。
- 证明全局同一时刻最多一个最高优先级 Banner，且包含影响、原因、时间、详情入口和已登记恢复动作。
- 证明 healthy、unknown、stale、degraded、blocked、query error 和恢复场景语义稳定。

## 执行副作用

- Go/Vue 测试只创建 synthetic fixture 和临时缓存；不会打开默认应用数据库或真实 Codex Home。
- frontend build 写 ignored `frontend/dist/`；Go/npm 可写语言缓存。
- 不触发网络、GitHub Actions、生产系统或正式发布；不读取或写入凭据。

## Acceptance Matrix

| 面 | 验证入口 | 通过条件 |
| --- | --- | --- |
| Projection contract | Go unit/binding tests | 七组件、唯一 primary、stale/failure/time 完整；只含有限 enum/数字 |
| App lifecycle | composition tests | health runtime 就绪后才绑定 projection reader；关闭/缺失/panic fail closed |
| Cache | Vue query/event tests | health invalidation、wake、foreground 只触发 refetch；无 event-derived 状态 |
| Sidebar | component tests | 总健康点、组件摘要、unknown/stale/error 均可区分且可进入本机状态 |
| Banner | arbitration/a11y tests | 最多一个；优先级稳定；影响/原因/时间/恢复/详情齐全 |
| Boundary | static/review | 无 raw error/path/content/token；不实现 TOO-283 完整 Data Health 或 Actions/release |

## Development Funnel

```bash
go test ./internal/app ./internal/health -count=20
CGO_ENABLED=0 go test ./internal/health -count=20
go test -race ./internal/app ./internal/health -count=10
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
git diff --check
```

## Integration Funnel

```bash
go test -p=1 ./... -count=1
go test -race -p=1 ./... -count=1
go vet ./...
go mod tidy -diff
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
make verify-architecture
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify-architecture
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify
git diff --check
```

## 场景步骤

1. 用 synthetic Projection 驱动 healthy/unknown/stale/degraded/blocked，并核对侧栏与 Banner 读取同一 query snapshot。
2. 并发构造 query error、stale 与高优先级 primary，核对 deterministic arbitration 且只渲染一个 Banner。
3. 触发详情与 recovery：retry 只 refetch；其余有限 action 导航本机状态锚点；失败保留就地反馈和导航。
4. 触发 health invalidation/wake/foreground，核对只失效 query cache，不通过 event payload 改写 UI 状态。
5. 运行 Go/前端/生成物/harness/package 全量验证并回写脱敏结果。

## 结果记录

| 步骤 | 结果 | 备注 |
| --- | --- | --- |
| scope/branch/lease | PASS | normal risk；独立分支和写租约已建立 |
| RED→GREEN | PASS | 先冻结 projection/binding/arbitration 场景，再实现 Go/Wails/Vue 闭环 |
| focused/race | PASS | app/health focused 20 次（health 另有 Pure Go 20 次）；affected race 10 次；非法 DTO、unknown/stale、动作失败均覆盖 |
| integration | PASS | 全仓 Go/full race/vet/tidy；frontend 49 文件/145 tests、typecheck/build；harness/project/generated/package 全通过 |
| implementation review | PASS | 首轮 6 个 blocking finding 已修复并由同一 reviewer closure；`remaining_blocking_findings=[]` |
| changelog/final scope | PASS | CHANGELOG Unreleased 已按 changelog-style 写入；不同 reviewer Final Scope Review 全项 PASS |
| PR/merge/post-merge | PASS | PR #51；feature commit `4ea83992`，merge commit `e5ea0279`；main 上 Go/full race/vet/tidy、frontend 49 files/145 tests、harness/project/generated、macOS arm64 minOS 15 ad-hoc app/ZIP 全部通过 |

补充证据：生成面为 1 service / 23 methods / 41 enums / 82 models / 1 event；macOS arm64、minimum OS 15.0 的 ad-hoc app 与 ZIP 验证通过。Go linker 仅输出仓库已知的本机 SDK 版本 warning，不影响测试或产物结论。

最终结果已回写 Linear TOO-282；Actions 保持 `actions_disabled_by_user`，未查询、触发或等待；普通 Execution 未发布。不得写入原始错误、绝对临时路径、真实用户标识或凭据。
