# M7-E6 本机状态与 Settings 页面验证 Runbook

## 当前验证结果

- 记录时间：2026-07-17
- 任务：TOO-277 `/local-status`、`/settings`、有限 typed controls 与 repair dry-run
- 当前结论：implementation、同 reviewer closure、完整 integration、package、修复后 browser visual 与 different final scope review 均 PASS；等待 PR/self-merge/post-merge，因此本卡暂不标最终 Done
- 计划：`.agents/plans/2026-07-17-too-277-local-status-settings.md`
- GitHub Actions：`actions_disabled_by_user`，不查询、不触发、不等待
- Release：普通 Execution，不创建 tag、GitHub Release、appcast 或正式发布产物

## 目标

验证两个页面只消费 generated runtime/settings DTO；Settings 保存、Home plan/confirm/recovery、pause/resume/reconcile 和 repair Analyze 通过有限 Wails commands 进入既有 domain state machine，且不泄露 path/credential/private identity/raw cause，不暴露 shell/SQL/repair Execute。

## 副作用与输出

- tests/build 写 Go/npm/Vitest/Vite cache、ignored `frontend/dist/`、`.task/`、`bin/`。
- temp E2E 只写 `$TMPDIR` 与 ignored `.agents/runs/too-277-*`；使用 synthetic Home/preferences/SQLite，不触达真实 `$HOME/.codex`。
- visual fixture 不进入 production route，截图只保存在 ignored runs；结束后停止 Vite/app/browser。
- 不访问 GitHub Actions，不执行 release；repair dry-run 不创建 job、backup 或 index/DB 写入。

## Acceptance Matrix

| 面 | 验证入口 | 通过条件 |
| --- | --- | --- |
| binding | app contract tests/generated | finite commands、redacted receipts、content-free errors、private types absent |
| settings | app runtime + Vue tests | CAS/no-op/conflict、readonly preserve、post-commit partial、authoritative refetch |
| Home | runtime + form tests | clean absolute input、plan preview、private latest-plan confirm、stale/cancel/recovery/path redaction |
| lifecycle | coordinator/runtime tests | pause_backfill/pause_all/resume/reconcile、overlap、close drain、invalidation |
| repair | temp Home/SQLite tests | Analyze only、zero writes、counts/noop、conflict、no Execute binding |
| status UI | view tests | Source/Job/Health normal/empty/partial/stale/fatal、fixed recovery labels、no identity |
| settings UI | view tests | metadata-driven editable fields、confirm dialogs、disabled/read-only、error/recovery |
| visual/native | isolated fixture/package | 1440 source、900 minimum、1120 package，无 overflow/伪事实/真实路径 |

## Verification Funnel

### Development / pre-review

```bash
go test ./internal/app ./internal/query ./internal/query/runtimeinfo ./internal/preferences ./internal/lifecycle ./internal/codex/index -count=1
go test -race ./internal/app ./internal/preferences ./internal/lifecycle ./internal/codex/index -count=1
npm --prefix frontend test -- --run src/features/runtime src/features/settings src/views/LocalStatusView.test.ts src/views/SettingsView.test.ts src/router/index.test.ts src/bindings/contracts.test.ts
npm --prefix frontend run typecheck
make harness-verify
git diff --check
```

### Post-integration

```bash
go test ./... -count=1
go test -race ./... -count=1
go vet ./...
go mod tidy -diff
CGO_ENABLED=0 go test ./internal/preferences ./internal/lifecycle ./internal/codex/index ./internal/store -count=1
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify
/usr/bin/python3 .agents/skills/project-version-release/scripts/project_version_release.py check --repo "$PWD" --json
git diff --check
```

## Fault / Privacy Checks

1. CAS conflict、durability/readback、post-commit quota reconcile failure 保留真实 committed snapshot 与可恢复反馈。
2. Home path 非绝对/未确认/物理替换/plan stale/cancel/close 均 fail closed；响应和 DOM 不含 path、device/inode、data-store key、plan/switch/attempt ID。
3. 两个并发 control command 由 admission + coordinator/private mutex 线性化；shutdown seal 后拒绝新命令并 drain 已接纳命令。
4. repair Analyze 前后比较 index bytes、Store jobs 与 backup root；必须完全不变。
5. staged diff 扫描 token/password/Authorization/path/secret；fixture 只用 synthetic marker。

## Visual / Native

1. 对照 `01-local-status.png` 与 `08-settings.png`，保留信息层级；删除 provider 不支持的演示开关与真实 path 展示。
2. 1440×1024 normal/partial/error，900×600 minimum；检查 focus、keyboard、live status、reduce motion 与 horizontal overflow。
3. isolated HOME/CODEX_HOME/TMP 启动 1120×720 packaged app；只使用 synthetic confirmed Home，不访问真实凭据/网络。

## 失败与恢复

| 失败点 | 停止条件 | 恢复 |
| --- | --- | --- |
| private/path leak | 不进入 review | 收窄 DTO/receipt，增加 generated/DOM negative test |
| settings partial commit | 不谎报失败或回滚 | refetch authoritative snapshot，展示 reconcile-required |
| Home stale/unknown durability | 不确认新 plan | 保留 recovery state，要求 Recover 或重新 plan |
| repair side effect | 立即阻断 | 保留 before/after evidence，移除写路径，只调用 Analyze |
| final review finding | 不 commit/PR | focused RED/GREEN、same reviewer closure、受影响 gate |

## 清理

- 删除 production route 外 fixture；停止 Vite/Wails/browser/app。
- 保留 ignored 最终截图/对照；不提交 temp Home、SQLite、backup、日志或真实运行 state。
- 未执行步骤不得标 PASS。

## 结果记录

### 已完成证据

- binding/domain：22 methods / 7 commands；Preferences private model、Home identity、repair Execute、shell/SQL/filesystem primitive 未进入 generated surface。
- Go focused：`go test ./internal/app ./internal/query ./internal/query/runtimeinfo ./internal/preferences ./internal/lifecycle ./internal/codex/index -count=1` PASS。
- Go focused race：`go test -race ./internal/app ./internal/preferences ./internal/lifecycle ./internal/codex/index -count=1` PASS。
- frontend focused：runtime/settings/views/router/binding 6 files / 22 tests PASS；frontend 全量在首次 review 前为 43 files / 117 tests PASS。
- frontend typecheck 与 `git diff --check` PASS。
- repair Analyze-only：测试比较 index bytes，并确认未创建 backup；零写入 PASS。
- browser visual：1440×1024 与 900×600 无横向 overflow、console error 或 page error；最终截图保存在 ignored `.agents/runs/too-277-status-settings-qa/`。
- 首次 independent implementation review：`REWORK_REQUIRED`；P1-1/P1-2/P2-1～P2-5 已各自形成 RED 证据，并由同一 reviewer closure 为 `blocking_findings=0`、`IMPLEMENTATION_REVIEW_PASS=YES`。
- 完整 Go：`go test ./... -count=1`、`go test -race ./... -count=1`、`go vet ./...`、`go mod tidy -diff` 全部 PASS；仅有非阻塞 macOS linker deployment-version warning。
- Pure-Go core：`CGO_ENABLED=0 go test ./internal/preferences ./internal/lifecycle ./internal/codex/index ./internal/store -count=1` PASS。
- frontend integration：43 files / 123 tests、typecheck 与 production build PASS。
- repo/package：`PATH="/tmp/codex-pulse-tools/bin:$PATH" make verify` PASS；generated 为 319 packages / 1 service / 22 methods / 39 enums / 80 models / 1 event 且稳定；arm64、minOS 15.0、ad-hoc bundle 与单顶层 ZIP 验证 PASS。
- version policy：`project_version_release.py check --json` 返回 `status=ok`、`findings=[]`；普通 Execution 未创建版本段、tag、release 或 appcast。
- 修复后 visual readback：Local Status 与 Settings 在 1440×1024、900×600 均无横向 overflow、page error、console error 或 private identity；Local Status 保持单一 health banner，截图保存在 ignored QA 目录。
- different final scope review：P0～P3 无 finding，`blocking_findings=[]`、`READY_FOR_INTEGRATION`；独立定点 Go 与前端 5 files / 20 tests 复验 PASS。
- GitHub Actions：`actions_disabled_by_user`，未查询、未触发、未等待。

### 待完成

- PR/self-merge/post-merge 与 Linear Done。
